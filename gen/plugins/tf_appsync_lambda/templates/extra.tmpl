data "aws_iam_policy_document" "lambda-{{$.Object}}-{{$.Field}}" {
  statement {
    effect = "Allow"

    actions = [
      "dynamodb:GetItem",
    ]

    resources = [
      aws_dynamodb_table.event.arn,
    ]
  }
}
